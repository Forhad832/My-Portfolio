import React, { useState } from 'react'

const Brand = ({filtered,brand}) => {
  const [active,setActive] = useState(0)
  return (
    <div>
      
     
     
    </div>
  )
}

export default Brand
